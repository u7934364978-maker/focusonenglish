/**
 * Unidad 36 B1 — So, such, so much, so many | Intensifiers
 * Gramática: so + adj, such + noun, so much/many
 * Vocabulario: intensifiers (very, really, extremely, etc.)
 */

import { UNIT_36_ALL_EXERCISES } from './unit-36-lessons-index';

export const UNIT_TITLE = 'So, Such, So Much, So Many | Intensifiers';

/** Todos los ejercicios de la Unidad 36 (6 lecciones). */
export const UNIT_36_EXERCISES = UNIT_36_ALL_EXERCISES;
